pipeline{

    agent any

    stages {

        stage('triggering jobs') {
            build job: 'bygg'
            build job: 'test', parameters: [string(name: 'p2', value: 'b')]
        }

    }
}
